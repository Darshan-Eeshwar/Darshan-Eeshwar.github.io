const b=document.querySelector('.menu'),n=document.querySelector('nav');if(b&&n)b.addEventListener('click',()=>n.classList.toggle('open'));document.querySelectorAll('[data-year]').forEach(e=>e.textContent=new Date().getFullYear());let ta=false;const lang=document.querySelector('.lang');if(lang)lang.addEventListener('click',()=>{ta=!ta;document.documentElement.lang=ta?'ta':'en';document.querySelectorAll('[data-en][data-ta]').forEach(e=>e.textContent=e.dataset[ta?'ta':'en']);lang.textContent=ta?'English':'தமிழ்';});document.querySelectorAll('[data-track]').forEach(e=>e.addEventListener('click',()=>{const eventName=e.dataset.track;window.dataLayer=window.dataLayer||[];window.dataLayer.push({event:'business_action',action:eventName});document.dispatchEvent(new CustomEvent('dimple:action',{detail:{action:eventName}}));}));const estimate=document.querySelector('#estimate-form'),estimateType=document.querySelector('#estimate-type'),estimateOutput=document.querySelector('#estimate-output'),estimateCta=document.querySelector('#estimate-cta');function updateEstimate(){if(!estimate)return;let total=Number(estimateType.value);estimate.querySelectorAll('input:checked').forEach(x=>total+=Number(x.value));const high=Math.ceil((total*1.25)/1000)*1000;estimateOutput.textContent=`Estimated starting range: ₹${total.toLocaleString('en-IN')}–₹${high.toLocaleString('en-IN')}`;estimateCta.dataset.estimate=`₹${total.toLocaleString('en-IN')}–₹${high.toLocaleString('en-IN')}`;}if(estimate){estimate.addEventListener('change',updateEstimate);updateEstimate();}const topButton=document.querySelector('.backtop');if(topButton){addEventListener('scroll',()=>topButton.classList.toggle('show',scrollY>600),{passive:true});topButton.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));}document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>n&&n.classList.remove('open')));if(estimateCta){estimateCta.addEventListener('click',()=>{const message=document.querySelector('textarea[name="message"]');if(message){message.value=`I used the website estimator. My estimated range is ${estimateCta.dataset.estimate||'to be discussed'}. Please contact me to confirm the scope.`;try{sessionStorage.setItem('dimple-estimate',message.value)}catch(e){}}});}const savedEstimate=(()=>{try{return sessionStorage.getItem('dimple-estimate')}catch(e){return null}})(),messageField=document.querySelector('textarea[name="message"]');if(savedEstimate&&messageField&&!messageField.value)messageField.value=savedEstimate;document.querySelectorAll('form').forEach(form=>form.addEventListener('submit',()=>{const source=form.querySelector('[name="source"]')?.value||'contact';window.dataLayer=window.dataLayer||[];window.dataLayer.push({event:'lead_submit',source});}));const themeButton=document.querySelector('.theme-toggle');let storedTheme=null;try{storedTheme=localStorage.getItem('dimple-theme')}catch(e){}if(storedTheme)document.documentElement.dataset.theme=storedTheme;if(themeButton)themeButton.addEventListener('click',()=>{const next=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=next;themeButton.setAttribute('aria-label',`Switch to ${next==='dark'?'light':'dark'} theme`);try{localStorage.setItem('dimple-theme',next)}catch(e){}});let storedLanguage=null;try{storedLanguage=localStorage.getItem('dimple-language')}catch(e){}if(storedLanguage==='ta'&&lang)lang.click();if(lang)lang.addEventListener('click',()=>{try{localStorage.setItem('dimple-language',document.documentElement.lang)}catch(e){}});const roiForm=document.querySelector('#roi-form'),roiOutput=document.querySelector('#roi-output');function updateRoi(){if(!roiForm)return;const visits=Math.max(0,Number(document.querySelector('#roi-visits').value)||0),rate=Math.min(100,Math.max(0,Number(document.querySelector('#roi-rate').value)||0)),value=Math.max(0,Number(document.querySelector('#roi-value').value)||0),leads=Math.round(visits*rate/100),opportunity=leads*value;roiOutput.textContent=`Potential opportunity: ${leads.toLocaleString('en-IN')} enquiries · ₹${opportunity.toLocaleString('en-IN')}`;}if(roiForm){roiForm.addEventListener('input',updateRoi);updateRoi();}const readiness=document.querySelector('#readiness-form'),readinessOutput=document.querySelector('#readiness-output');function updateReadiness(){if(!readiness)return;const all=readiness.querySelectorAll('input[type="checkbox"]'),done=readiness.querySelectorAll('input:checked');readinessOutput.textContent=`Readiness: ${done.length} of ${all.length}`;}if(readiness){readiness.addEventListener('change',updateReadiness);updateReadiness();}const estimateShare=document.querySelector('#estimate-share');function updateEstimateShare(){if(!estimateShare||!estimateCta)return;const msg=`Hello Dimple Enterprises, I used your website estimator. My indicative range is ${estimateCta.dataset.estimate||'to be discussed'}. Please help me confirm the scope.`;estimateShare.href=`https://wa.me/919566911041?text=${encodeURIComponent(msg)}`;}if(estimate){estimate.addEventListener('change',updateEstimateShare);updateEstimateShare();}document.querySelectorAll('form[method="POST"]').forEach(form=>form.addEventListener('submit',()=>{const button=form.querySelector('button[type="submit"]');if(button){button.dataset.original=button.textContent;button.textContent='Sending…';button.setAttribute('aria-busy','true');}}));const shareSite=document.querySelector('.share-site'),shareToast=document.querySelector('.share-toast');if(shareSite)shareSite.addEventListener('click',async()=>{const data={title:'Dimple Enterprises',text:'Websites and digital growth solutions for businesses.',url:location.origin+location.pathname};try{if(navigator.share){await navigator.share(data);if(shareToast)shareToast.textContent='Website shared.';}else{await navigator.clipboard.writeText(data.url);if(shareToast)shareToast.textContent='Website link copied.';}}catch(e){if(e.name!=='AbortError'&&shareToast)shareToast.textContent='Copy this link: '+data.url;}});const faqSearch=document.querySelector('#faq-search'),faqItems=[...document.querySelectorAll('#faq details')],faqEmpty=document.querySelector('#faq-empty');if(faqSearch)faqSearch.addEventListener('input',()=>{const query=faqSearch.value.trim().toLowerCase();let visible=0;faqItems.forEach(item=>{const match=!query||item.textContent.toLowerCase().includes(query);item.hidden=!match;if(match)visible++;});if(faqEmpty)faqEmpty.hidden=visible>0;});const contactForm=document.querySelector('#contact form'),draftStatus=document.querySelector('#draft-status'),clearDraft=document.querySelector('#clear-draft'),draftKey='dimple-enquiry-session';if(contactForm){try{const saved=JSON.parse(sessionStorage.getItem(draftKey)||'{}');Object.entries(saved).forEach(([name,value])=>{const field=contactForm.elements.namedItem(name);if(field&&typeof value==='string'&&!field.value)field.value=value;});if(Object.keys(saved).length&&draftStatus)draftStatus.textContent='Draft restored from this browser session.';}catch(e){}let draftTimer;contactForm.addEventListener('input',()=>{clearTimeout(draftTimer);draftTimer=setTimeout(()=>{const data={};new FormData(contactForm).forEach((value,key)=>{if(!key.startsWith('_'))data[key]=String(value)});try{sessionStorage.setItem(draftKey,JSON.stringify(data));if(draftStatus)draftStatus.textContent='Draft saved in this browser session.';}catch(e){}},400)});contactForm.addEventListener('submit',()=>{try{sessionStorage.removeItem(draftKey)}catch(e){}});}if(clearDraft)clearDraft.addEventListener('click',()=>{try{sessionStorage.removeItem(draftKey)}catch(e){}if(contactForm)contactForm.reset();if(draftStatus)draftStatus.textContent='Draft cleared.';});document.querySelectorAll('[data-display]').forEach(button=>button.addEventListener('click',()=>{const mode=button.dataset.display;if(mode==='text')document.documentElement.classList.toggle('large-text');if(mode==='contrast')document.documentElement.classList.toggle('high-contrast');if(mode==='reset')document.documentElement.classList.remove('large-text','high-contrast');}));const copyStatus=document.querySelector('.copy-status');document.querySelectorAll('.copy-contact').forEach(button=>button.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(button.dataset.copy);if(copyStatus)copyStatus.textContent='Contact detail copied.';}catch(e){if(copyStatus)copyStatus.textContent='Select the contact detail above to copy it.';}}));const downloadContact=document.querySelector('.download-contact');if(downloadContact)downloadContact.addEventListener('click',()=>{const card='BEGIN:VCARD\r\nVERSION:3.0\r\nFN:Dimple Enterprises\r\nORG:Dimple Enterprises\r\nTEL;TYPE=WORK,VOICE:+916380387507\r\nEMAIL:starwaltcompany@gmail.com\r\nURL:https://darshan-eeshwar.github.io/\r\nADR;TYPE=WORK:;;Vellore;Tamil Nadu;;;India\r\nEND:VCARD\r\n',blob=new Blob([card],{type:'text/vcard'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Dimple-Enterprises.vcf';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);if(copyStatus)copyStatus.textContent='Business contact downloaded.';});const connectionStatus=document.querySelector('.connection-status'),updateStatus=document.querySelector('.update-status'),updateButton=updateStatus?.querySelector('button'),installButton=document.querySelector('#install-app');function showConnection(){if(!connectionStatus)return;connectionStatus.hidden=navigator.onLine;connectionStatus.textContent=navigator.onLine?'Back online.':'You are offline. Previously visited pages may still work.';if(navigator.onLine){connectionStatus.hidden=false;setTimeout(()=>connectionStatus.hidden=true,2500);}}addEventListener('online',showConnection);addEventListener('offline',showConnection);if(!navigator.onLine)showConnection();let installPrompt=null;addEventListener('beforeinstallprompt',event=>{event.preventDefault();installPrompt=event;if(installButton)installButton.hidden=false;});if(installButton)installButton.addEventListener('click',async()=>{if(!installPrompt)return;installPrompt.prompt();await installPrompt.userChoice;installPrompt=null;installButton.hidden=true;});if('serviceWorker'in navigator)addEventListener('load',async()=>{try{const registration=await navigator.serviceWorker.register('./service-worker.js');if(registration.waiting&&updateStatus)updateStatus.hidden=false;registration.addEventListener('updatefound',()=>{const worker=registration.installing;if(worker)worker.addEventListener('statechange',()=>{if(worker.state==='installed'&&navigator.serviceWorker.controller&&updateStatus)updateStatus.hidden=false;});});if(updateButton)updateButton.addEventListener('click',()=>registration.waiting?.postMessage({type:'SKIP_WAITING'}));navigator.serviceWorker.addEventListener('controllerchange',()=>location.reload());}catch(e){console.warn('Offline support unavailable',e);}});const params=new URLSearchParams(location.search),utmKeys=['utm_source','utm_medium','utm_campaign'];let attribution={landing_page:location.pathname};utmKeys.forEach(key=>{const value=params.get(key);if(value)attribution[key]=value.slice(0,120)});try{const old=JSON.parse(sessionStorage.getItem('dimple-attribution')||'{}');attribution={...old,...attribution};sessionStorage.setItem('dimple-attribution',JSON.stringify(attribution));}catch(e){}document.querySelectorAll('form[method="POST"]').forEach(form=>{Object.entries(attribution).forEach(([key,value])=>{let input=form.querySelector(`[name="${key}"]`);if(!input){input=document.createElement('input');input.type='hidden';input.name=key;form.appendChild(input)}input.value=value;});});const consultForm=document.querySelector('#consultation-builder'),consultDate=document.querySelector('#consult-date'),consultLink=document.querySelector('#consult-link'),consultPreview=document.querySelector('#consult-preview');if(consultDate){const tomorrow=new Date();tomorrow.setDate(tomorrow.getDate()+1);consultDate.min=tomorrow.toISOString().slice(0,10);}function updateConsult(){if(!consultForm)return;const data=new FormData(consultForm),date=data.get('date')||'a suitable date',time=data.get('time')||'a suitable time',service=data.get('service')||'digital services',name=data.get('name')||'a prospective customer',message=`Hello Dimple Enterprises, I am ${name}. I would like to request a consultation about ${service} on ${date} at ${time} IST. Please confirm whether this time is available.`;if(consultPreview)consultPreview.textContent=message;if(consultLink)consultLink.href=`https://wa.me/919566911041?text=${encodeURIComponent(message)}`;}if(consultForm){consultForm.addEventListener('input',updateConsult);updateConsult();}const briefForm=document.querySelector('#brief-builder'),briefPreview=document.querySelector('#brief-preview'),briefDownload=document.querySelector('#brief-download');function buildBrief(){if(!briefForm)return'';const data=new FormData(briefForm),features=data.getAll('features'),goals=data.getAll('goals');return `DIMPLE ENTERPRISES - PROJECT BRIEF\n\nBusiness: ${data.get('business')||'Not entered'}\nContact: ${data.get('contact')||'Not entered'}\nProject type: ${data.get('project_type')||'Not selected'}\nPrimary goals: ${goals.length?goals.join(', '):'Not selected'}\nRequested features: ${features.length?features.join(', '):'Not selected'}\nTarget timeline: ${data.get('timeline')||'Not selected'}\nBudget range: ${data.get('budget')||'Not selected'}\n\nNotes:\n${data.get('notes')||'None'}\n\nGenerated at https://darshan-eeshwar.github.io/project-brief.html`; }function updateBrief(){const text=buildBrief();if(briefPreview)briefPreview.textContent=text;}if(briefForm){briefForm.addEventListener('input',updateBrief);updateBrief();}if(briefDownload)briefDownload.addEventListener('click',()=>{const blob=new Blob([buildBrief()],{type:'text/plain'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Dimple-Enterprises-Project-Brief.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});const readingProgress=document.querySelector('.reading-progress');if(readingProgress)addEventListener('scroll',()=>{const max=document.documentElement.scrollHeight-innerHeight,progress=max>0?scrollY/max*100:0;readingProgress.style.width=`${Math.min(100,progress)}%`;},{passive:true});document.addEventListener('keydown',event=>{if(event.key==='/'&&!event.ctrlKey&&!event.metaKey&&!event.altKey&&!['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName)){event.preventDefault();const search=document.querySelector('#site-search');if(search)search.focus();else location.href='/search.html';}});const searchForm=document.querySelector('#search-form'),searchInput=document.querySelector('#site-search'),searchResults=document.querySelector('#search-results'),searchSummary=document.querySelector('#search-summary'),recentWrap=document.querySelector('#recent-searches');let searchIndex=[];function addHighlighted(parent,text,terms){let remaining=text,lower=text.toLowerCase(),position=0;const hits=[];terms.forEach(term=>{let start=0,index;while((index=lower.indexOf(term,start))>-1){hits.push([index,index+term.length]);start=index+term.length;}});hits.sort((a,b)=>a[0]-b[0]);const merged=[];hits.forEach(hit=>{const last=merged[merged.length-1];if(last&&hit[0]<=last[1])last[1]=Math.max(last[1],hit[1]);else merged.push(hit);});merged.forEach(([start,end])=>{if(start>position)parent.append(text.slice(position,start));const mark=document.createElement('mark');mark.textContent=text.slice(start,end);parent.append(mark);position=end;});if(position<text.length)parent.append(text.slice(position));}function renderRecent(){if(!recentWrap)return;let recent=[];try{recent=JSON.parse(sessionStorage.getItem('dimple-searches')||'[]')}catch(e){}recentWrap.replaceChildren();recent.forEach(query=>{const button=document.createElement('button');button.type='button';button.textContent=query;button.addEventListener('click',()=>{searchInput.value=query;runSearch(query)});recentWrap.append(button);});}function saveRecent(query){try{let recent=JSON.parse(sessionStorage.getItem('dimple-searches')||'[]');recent=[query,...recent.filter(x=>x!==query)].slice(0,5);sessionStorage.setItem('dimple-searches',JSON.stringify(recent));renderRecent();}catch(e){}}function runSearch(raw){if(!searchResults)return;const query=raw.trim().toLowerCase(),terms=query.split(/\s+/).filter(Boolean);searchResults.replaceChildren();if(!terms.length){searchSummary.textContent='Enter a word or phrase to search.';return;}const found=searchIndex.map(item=>{const title=item.title.toLowerCase(),description=item.description.toLowerCase(),keywords=item.keywords.toLowerCase();let score=0;terms.forEach(term=>{if(title.includes(term))score+=5;if(keywords.includes(term))score+=3;if(description.includes(term))score+=1;});return{item,score};}).filter(x=>x.score>0).sort((a,b)=>b.score-a.score);searchSummary.textContent=`${found.length} result${found.length===1?'':'s'} for “${raw.trim()}”`;found.forEach(({item})=>{const link=document.createElement('a');link.className='search-result';link.href=item.url;const title=document.createElement('b'),desc=document.createElement('p'),type=document.createElement('small');addHighlighted(title,item.title,terms);addHighlighted(desc,item.description,terms);type.textContent=item.type;link.append(title,desc,type);searchResults.append(link);});saveRecent(raw.trim());const url=new URL(location.href);url.searchParams.set('q',raw.trim());history.replaceState(null,'',url);}if(searchForm){fetch('search-index.json').then(r=>r.json()).then(data=>{searchIndex=data;const initial=new URLSearchParams(location.search).get('q')||'';searchInput.value=initial;if(initial)runSearch(initial);else searchSummary.textContent='Search services, tools, policies and guides.';}).catch(()=>searchSummary.textContent='Search index is unavailable. Please try again online.');searchForm.addEventListener('submit',event=>{event.preventDefault();runSearch(searchInput.value)});renderRecent();}const assessment=document.querySelector('#readiness-assessment'),assessmentProgress=document.querySelector('#assessment-progress span'),assessmentStatus=document.querySelector('#assessment-status'),assessmentResult=document.querySelector('#assessment-result'),scoreRing=document.querySelector('#score-ring'),scoreLabel=document.querySelector('#score-label'),scoreSummary=document.querySelector('#score-summary'),recommendationList=document.querySelector('#recommendations'),downloadAssessment=document.querySelector('#download-assessment'),resetAssessment=document.querySelector('#reset-assessment');const assessmentAdvice={website:'Create or improve a mobile-friendly website with clear services and contact actions.',speed:'Review image sizes, scripts and hosting to improve mobile loading performance.',contact:'Make phone, WhatsApp and enquiry actions easy to find and test them regularly.',profile:'Complete and maintain an accurate Google Business Profile.',reviews:'Build a consistent process for requesting genuine customer reviews.',content:'Publish clear service information and useful answers to customer questions.',security:'Use HTTPS, strong access controls, backups and multi-factor authentication.',analytics:'Measure qualified enquiries, calls and messages with privacy-conscious analytics.',followup:'Define who responds to new leads and how quickly follow-up happens.',brand:'Use consistent business names, colours, messaging and contact details.',privacy:'Publish appropriate privacy information and collect only necessary form data.',maintenance:'Schedule content, security and dependency reviews after launch.'};let assessmentReport='';function updateAssessmentProgress(){if(!assessment)return;const answered=new Set([...new FormData(assessment).keys()]).size,total=assessment.querySelectorAll('fieldset').length,percent=Math.round(answered/total*100);if(assessmentProgress)assessmentProgress.style.width=`${percent}%`;if(assessmentStatus)assessmentStatus.textContent=`${answered} of ${total} questions answered`;}function calculateAssessment(event){event.preventDefault();const data=new FormData(assessment),questions=[...assessment.querySelectorAll('fieldset')];if(new Set([...data.keys()]).size<questions.length){if(assessmentStatus)assessmentStatus.textContent='Please answer every question before calculating your score.';return;}let points=0,missing=[];questions.forEach(group=>{const value=Number(data.get(group.dataset.key));points+=value;if(value===0)missing.push(group.dataset.key)});const score=Math.round(points/(questions.length*2)*100);let level=score>=80?'Strong foundation':score>=55?'Developing foundation':'Priority improvements needed';const advice=missing.length?missing.slice(0,6).map(key=>assessmentAdvice[key]):['Maintain the current foundation, review performance regularly and keep information accurate.'];scoreRing.style.setProperty('--score',`${score}%`);scoreRing.querySelector('span').textContent=`${score}%`;scoreLabel.textContent=level;scoreSummary.textContent=score>=80?'Your digital basics are well covered. Focus on measurement, testing and continuous improvement.':score>=55?'Several foundations are working, but closing the gaps below can improve credibility and enquiries.':'Start with the recommendations below to create a safer, clearer and more discoverable digital presence.';recommendationList.replaceChildren(...advice.map(text=>{const li=document.createElement('li');li.textContent=text;return li}));assessmentResult.hidden=false;assessmentReport=`DIMPLE ENTERPRISES - DIGITAL READINESS REPORT\n\nScore: ${score}% - ${level}\n\nPriority recommendations:\n${advice.map((text,i)=>`${i+1}. ${text}`).join('\n')}\n\nThis self-assessment is an educational planning aid, not a technical audit or guarantee.\nGenerated at https://darshan-eeshwar.github.io/readiness-assessment.html`;assessmentResult.scrollIntoView({behavior:'smooth',block:'start'});}if(assessment){assessment.addEventListener('change',updateAssessmentProgress);assessment.addEventListener('submit',calculateAssessment);updateAssessmentProgress();}if(downloadAssessment)downloadAssessment.addEventListener('click',()=>{if(!assessmentReport)return;const blob=new Blob([assessmentReport],{type:'text/plain'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Dimple-Enterprises-Digital-Readiness-Report.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});if(resetAssessment)resetAssessment.addEventListener('click',()=>{assessment.reset();assessmentResult.hidden=true;assessmentReport='';updateAssessmentProgress();scrollTo({top:0,behavior:'smooth'});});const quoteCompare=document.querySelector('#quote-comparison'),vendorAResult=document.querySelector('#vendor-a-result'),vendorBResult=document.querySelector('#vendor-b-result'),comparisonSummary=document.querySelector('#comparison-summary'),downloadComparison=document.querySelector('#download-comparison');let comparisonReport='';function scoreVendor(prefix){const boxes=[...quoteCompare.querySelectorAll(`[data-vendor="${prefix}"] input[type="checkbox"]`)],score=boxes.filter(x=>x.checked).length,total=boxes.length,cost=Number(quoteCompare.querySelector(`[name="${prefix}_cost"]`).value)||0,name=quoteCompare.querySelector(`[name="${prefix}_name"]`).value||`Vendor ${prefix.toUpperCase()}`;return{name,cost,score,total,percent:Math.round(score/total*100)}}function updateComparison(){if(!quoteCompare)return;const a=scoreVendor('a'),b=scoreVendor('b');vendorAResult.innerHTML=`<b>${a.percent}% complete</b><span>${a.name} · ₹${a.cost.toLocaleString('en-IN')}</span>`;vendorBResult.innerHTML=`<b>${b.percent}% complete</b><span>${b.name} · ₹${b.cost.toLocaleString('en-IN')}</span>`;const message=a.percent===b.percent?'Both proposals currently cover the same number of comparison points. Review scope detail, exclusions and fit.':`${a.percent>b.percent?a.name:b.name} currently covers more of the comparison points. Price alone should not decide the final choice.`;comparisonSummary.textContent=message;comparisonReport=`DIMPLE ENTERPRISES - WEBSITE QUOTE COMPARISON\n\n${a.name}: ₹${a.cost.toLocaleString('en-IN')} · ${a.score}/${a.total} points (${a.percent}%)\n${b.name}: ₹${b.cost.toLocaleString('en-IN')} · ${b.score}/${b.total} points (${b.percent}%)\n\nSummary: ${message}\n\nThis tool compares declared proposal completeness only. It does not verify quality, capability or legal terms.\nGenerated at https://darshan-eeshwar.github.io/quote-comparison.html`; }if(quoteCompare){quoteCompare.addEventListener('input',updateComparison);updateComparison();}if(downloadComparison)downloadComparison.addEventListener('click',()=>{const blob=new Blob([comparisonReport],{type:'text/plain'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Website-Quote-Comparison.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});const timelinePlanner=document.querySelector('#timeline-planner'),timelineStart=document.querySelector('#timeline-start'),timelineType=document.querySelector('#timeline-type'),milestoneList=document.querySelector('#milestone-list'),timelineSummary=document.querySelector('#timeline-summary'),downloadTimeline=document.querySelector('#download-timeline'),downloadCalendar=document.querySelector('#download-calendar');let plannedMilestones=[];const timelinePlans={starter:[['Discovery and goals',2],['Content and structure',3],['Design and build',6],['Review and revisions',3],['Testing and launch',2]],business:[['Discovery and goals',3],['Content and architecture',5],['Design direction',5],['Development',10],['Review and revisions',5],['Testing and launch',3]],commerce:[['Discovery and catalogue planning',5],['Content and data preparation',7],['Experience design',7],['Development and integration',14],['Review and revisions',7],['Testing and launch',5]]};function addBusinessDays(date,days){const result=new Date(date);let added=0;while(added<days){result.setDate(result.getDate()+1);if(result.getDay()!==0&&result.getDay()!==6)added++;}return result;}function localDate(value){const [y,m,d]=value.split('-').map(Number);return new Date(y,m-1,d,12);}function formatDate(date){return date.toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'});}function icsDate(date){return `${date.getFullYear()}${String(date.getMonth()+1).padStart(2,'0')}${String(date.getDate()).padStart(2,'0')}`;}function updateTimeline(){if(!timelinePlanner||!timelineStart.value)return;let cursor=localDate(timelineStart.value);const plan=timelinePlans[timelineType.value],start=new Date(cursor);plannedMilestones=plan.map(([name,days])=>{const phaseStart=new Date(cursor);cursor=addBusinessDays(cursor,days);return{name,days,start:phaseStart,end:new Date(cursor)}});milestoneList.replaceChildren(...plannedMilestones.map(item=>{const li=document.createElement('li'),time=document.createElement('time'),wrap=document.createElement('div'),title=document.createElement('b'),detail=document.createElement('small');time.dateTime=item.end.toISOString().slice(0,10);time.textContent=formatDate(item.end);title.textContent=item.name;detail.textContent=`${item.days} working day${item.days===1?'':'s'} · ${formatDate(item.start)} to ${formatDate(item.end)}`;wrap.append(title,detail);li.append(time,wrap);return li;}));const total=plan.reduce((sum,x)=>sum+x[1],0);timelineSummary.textContent=`Indicative plan: ${total} working days · ${formatDate(start)} to ${formatDate(cursor)}. Weekends are excluded; holidays, content delays and approval time may change the schedule.`;}if(timelineStart){const next=new Date();next.setDate(next.getDate()+1);timelineStart.min=next.toISOString().slice(0,10);timelineStart.value=next.toISOString().slice(0,10);}if(timelinePlanner){timelinePlanner.addEventListener('input',updateTimeline);updateTimeline();}if(downloadTimeline)downloadTimeline.addEventListener('click',()=>{const text=`DIMPLE ENTERPRISES - INDICATIVE PROJECT TIMELINE\n\n${timelineSummary.textContent}\n\n${plannedMilestones.map((m,i)=>`${i+1}. ${m.name}: ${formatDate(m.start)} to ${formatDate(m.end)} (${m.days} working days)`).join('\n')}\n\nPlanning estimate only; final delivery dates are confirmed after scope, content and dependencies are reviewed.`;const blob=new Blob([text],{type:'text/plain'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Dimple-Enterprises-Project-Timeline.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});if(downloadCalendar)downloadCalendar.addEventListener('click',()=>{const events=plannedMilestones.map((m,i)=>`BEGIN:VEVENT\r\nUID:dimple-${i}-${icsDate(m.start)}@darshan-eeshwar.github.io\r\nDTSTART;VALUE=DATE:${icsDate(m.start)}\r\nDTEND;VALUE=DATE:${icsDate(addBusinessDays(m.end,1))}\r\nSUMMARY:Dimple Enterprises - ${m.name}\r\nDESCRIPTION:Indicative project milestone; confirm final dates after scope review.\r\nEND:VEVENT`).join('\r\n');const calendar=`BEGIN:VCALENDAR\r\nVERSION:2.0\r\nPRODID:-//Dimple Enterprises//Timeline Planner//EN\r\nCALSCALE:GREGORIAN\r\n${events}\r\nEND:VCALENDAR\r\n`,blob=new Blob([calendar],{type:'text/calendar'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Dimple-Enterprises-Project-Timeline.ics';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});const brandForm=document.querySelector('#brand-planner'),brandName=document.querySelector('#brand-name'),brandTagline=document.querySelector('#brand-tagline'),brandPrimary=document.querySelector('#brand-primary'),brandBackground=document.querySelector('#brand-background'),brandText=document.querySelector('#brand-text'),brandPreview=document.querySelector('#brand-preview'),brandPreviewName=document.querySelector('#brand-preview-name'),brandPreviewTagline=document.querySelector('#brand-preview-tagline'),brandButton=document.querySelector('#brand-sample-button'),contrastValue=document.querySelector('#contrast-value'),contrastNormal=document.querySelector('#contrast-normal'),contrastLarge=document.querySelector('#contrast-large'),brandSwatches=document.querySelector('#brand-swatches'),downloadBrandNotes=document.querySelector('#download-brand-notes'),downloadBrandCss=document.querySelector('#download-brand-css');let brandRatio=0;function hexRgb(hex){const clean=hex.replace('#','');return[0,2,4].map(i=>parseInt(clean.slice(i,i+2),16)/255)}function luminance(hex){const rgb=hexRgb(hex).map(v=>v<=.04045?v/12.92:Math.pow((v+.055)/1.055,2.4));return .2126*rgb[0]+.7152*rgb[1]+.0722*rgb[2]}function contrast(a,b){const l1=luminance(a),l2=luminance(b);return(Math.max(l1,l2)+.05)/(Math.min(l1,l2)+.05)}function downloadText(name,text,type='text/plain'){const blob=new Blob([text],{type}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}function updateBrand(){if(!brandForm)return;const primary=brandPrimary.value,background=brandBackground.value,text=brandText.value;brandPreview.style.background=background;brandPreview.style.color=text;brandButton.style.background=primary;brandButton.style.color=contrast(primary,'#ffffff')>=4.5?'#ffffff':'#000000';brandPreviewName.textContent=brandName.value||'Your Business';brandPreviewTagline.textContent=brandTagline.value||'A clear promise for your customers.';brandRatio=contrast(text,background);contrastValue.textContent=`${brandRatio.toFixed(2)}:1`;const normalPass=brandRatio>=4.5,largePass=brandRatio>=3;contrastNormal.textContent=normalPass?'Passes AA for normal text':'Does not pass AA for normal text';contrastLarge.textContent=largePass?'Passes AA for large text':'Does not pass AA for large text';contrastNormal.className=normalPass?'contrast-pass':'contrast-fail';contrastLarge.className=largePass?'contrast-pass':'contrast-fail';const colours=[['Primary',primary],['Background',background],['Text',text]];brandSwatches.replaceChildren(...colours.map(([label,value])=>{const span=document.createElement('span');span.style.background=value;span.style.color=contrast(value,'#ffffff')>=4.5?'#ffffff':'#000000';span.textContent=`${label}\n${value.toUpperCase()}`;return span;}));}if(brandForm){brandForm.addEventListener('input',updateBrand);updateBrand();}if(downloadBrandNotes)downloadBrandNotes.addEventListener('click',()=>downloadText('Brand-Starter-Notes.txt',`BRAND STARTER NOTES\n\nBusiness: ${brandName.value}\nTagline: ${brandTagline.value}\nPrimary: ${brandPrimary.value}\nBackground: ${brandBackground.value}\nText: ${brandText.value}\nText/background contrast: ${brandRatio.toFixed(2)}:1\nNormal text AA: ${brandRatio>=4.5?'Pass':'Fail'}\nLarge text AA: ${brandRatio>=3?'Pass':'Fail'}\n\nThis palette check covers colour contrast only and is not a complete accessibility audit.`));if(downloadBrandCss)downloadBrandCss.addEventListener('click',()=>downloadText('brand-colours.css',`:root {\n  --brand-primary: ${brandPrimary.value};\n  --brand-background: ${brandBackground.value};\n  --brand-text: ${brandText.value};\n}\n`,'text/css'));const snippetForm=document.querySelector('#snippet-planner'),snippetTitle=document.querySelector('#snippet-title'),snippetDescription=document.querySelector('#snippet-description'),snippetDomain=document.querySelector('#snippet-domain'),snippetSlug=document.querySelector('#snippet-slug'),snippetTitleCount=document.querySelector('#snippet-title-count'),snippetDescriptionCount=document.querySelector('#snippet-description-count'),searchPreviewUrl=document.querySelector('#search-preview-url'),searchPreviewTitle=document.querySelector('#search-preview-title'),searchPreviewDescription=document.querySelector('#search-preview-description'),socialPreviewTitle=document.querySelector('#social-preview-title'),socialPreviewDescription=document.querySelector('#social-preview-description'),socialPreviewDomain=document.querySelector('#social-preview-domain'),metadataOutput=document.querySelector('#metadata-output'),downloadMetadata=document.querySelector('#download-metadata'),downloadSeoBrief=document.querySelector('#download-seo-brief');let generatedMetadata='';function safeUrlPart(value){return value.trim().toLowerCase().replace(/[^a-z0-9\s-]/g,'').replace(/\s+/g,'-').replace(/-+/g,'-').replace(/^-|-$/g,'')}function escapeAttr(value){return value.replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}function counterState(element,length,good,limit){element.textContent=`${length} / ${limit} characters`;element.className='char-count '+(length<=good?'good':length<=limit?'warn':'over')}function updateSnippet(){if(!snippetForm)return;const title=snippetTitle.value.trim()||'Your page title',description=snippetDescription.value.trim()||'A clear, useful description of this page.',domain=snippetDomain.value.trim().replace(/^https?:\/\//,'').replace(/\/$/,'')||'example.com',slug=safeUrlPart(snippetSlug.value)||'page',url=`https://${domain}/${slug}`;counterState(snippetTitleCount,snippetTitle.value.length,55,60);counterState(snippetDescriptionCount,snippetDescription.value.length,150,160);searchPreviewUrl.textContent=url;searchPreviewTitle.textContent=title;searchPreviewDescription.textContent=description;socialPreviewTitle.textContent=title;socialPreviewDescription.textContent=description;socialPreviewDomain.textContent=domain;generatedMetadata=`<title>${escapeAttr(title)}</title>\n<meta name="description" content="${escapeAttr(description)}">\n<link rel="canonical" href="${escapeAttr(url)}">\n<meta property="og:type" content="website">\n<meta property="og:title" content="${escapeAttr(title)}">\n<meta property="og:description" content="${escapeAttr(description)}">\n<meta property="og:url" content="${escapeAttr(url)}">\n<meta name="twitter:card" content="summary_large_image">\n<meta name="twitter:title" content="${escapeAttr(title)}">\n<meta name="twitter:description" content="${escapeAttr(description)}">`;metadataOutput.textContent=generatedMetadata;}if(snippetForm){snippetForm.addEventListener('input',updateSnippet);snippetSlug.addEventListener('blur',()=>{snippetSlug.value=safeUrlPart(snippetSlug.value);updateSnippet()});updateSnippet();}if(downloadMetadata)downloadMetadata.addEventListener('click',()=>downloadText('page-metadata.html',generatedMetadata,'text/html'));if(downloadSeoBrief)downloadSeoBrief.addEventListener('click',()=>downloadText('SEO-Snippet-Brief.txt',`SEO SNIPPET BRIEF\n\nTitle: ${snippetTitle.value}\nTitle length: ${snippetTitle.value.length}\nDescription: ${snippetDescription.value}\nDescription length: ${snippetDescription.value.length}\nDomain: ${snippetDomain.value}\nSlug: ${safeUrlPart(snippetSlug.value)}\n\nCharacter counts are planning guidance only. Search engines may rewrite or truncate displayed snippets. Metadata does not guarantee rankings.`));const solutionFinder=document.querySelector('#solution-finder'),finderResult=document.querySelector('#finder-result'),finderPackage=document.querySelector('#finder-package'),finderSummary=document.querySelector('#finder-summary'),finderReasons=document.querySelector('#finder-reasons'),finderCaution=document.querySelector('#finder-caution'),downloadRecommendation=document.querySelector('#download-recommendation'),shareRecommendation=document.querySelector('#share-recommendation');let recommendationText='';function calculateSolution(event){event.preventDefault();const data=new FormData(solutionFinder),required=['goal','pages','commerce','content','growth','budget'];if(required.some(key=>!data.get(key))){finderResult.hidden=false;finderPackage.textContent='Complete all questions';finderSummary.textContent='Choose one answer for every question to receive a recommendation.';finderReasons.replaceChildren();finderCaution.textContent='';return;}let score=0,reasons=[];const commerce=data.get('commerce')==='yes';if(data.get('goal')==='sell'){score+=4;reasons.push('Online selling is a primary goal.')}if(data.get('goal')==='leads'){score+=2;reasons.push('The website needs a stronger enquiry journey.')}if(data.get('pages')==='many'){score+=2;reasons.push('The expected content needs a multi-page structure.')}if(data.get('content')==='help'){score+=1;reasons.push('Content planning support is needed.')}if(data.get('growth')==='yes'){score+=2;reasons.push('SEO and analytics foundations are important.')}if(commerce){score+=5;reasons.push('A product catalogue or commerce workflow is required.')}let packageName=commerce||score>=8?'Commerce solution':score>=4?'Business Growth':'Starter Presence';const descriptions={'Starter Presence':'A focused mobile-friendly presence with clear services, WhatsApp and enquiry actions.','Business Growth':'A multi-page website with richer content, local SEO foundations and analytics-ready actions.','Commerce solution':'A catalogue or commerce-focused experience with order, payment and integration planning.'};const budget=data.get('budget');let caution='Final scope and price require a requirements review.';if(packageName==='Commerce solution'&&budget==='under20')caution='Your selected commerce needs are unlikely to fit the lowest budget range. Consider a phased catalogue-first approach.';else if(packageName==='Business Growth'&&budget==='under20')caution='Your goals suggest a Growth solution, but the selected budget may require a smaller first phase.';else if(packageName==='Starter Presence'&&(budget==='50to100'||budget==='above100'))caution='Your budget may support a broader Growth solution; confirm whether additional content or integrations would add value.';finderPackage.textContent=packageName;finderSummary.textContent=descriptions[packageName];finderReasons.replaceChildren(...reasons.map(reason=>{const li=document.createElement('li');li.textContent=reason;return li}));finderCaution.textContent=caution;finderResult.hidden=false;recommendationText=`DIMPLE ENTERPRISES - SOLUTION FINDER\n\nRecommended starting point: ${packageName}\n${descriptions[packageName]}\n\nWhy:\n${reasons.map((r,i)=>`${i+1}. ${r}`).join('\n')}\n\nBudget note: ${caution}\n\nThis is a rule-based starting recommendation, not a final scope or quote.`;shareRecommendation.href=`https://wa.me/919566911041?text=${encodeURIComponent(`Hello Dimple Enterprises, your solution finder recommended ${packageName}. I would like to discuss the scope. ${caution}`)}`;finderResult.scrollIntoView({behavior:'smooth',block:'start'});}if(solutionFinder)solutionFinder.addEventListener('submit',calculateSolution);if(downloadRecommendation)downloadRecommendation.addEventListener('click',()=>{if(recommendationText)downloadText('Dimple-Enterprises-Solution-Recommendation.txt',recommendationText)});const maintenance=document.querySelector('#maintenance-planner'),maintenanceStart=document.querySelector('#maintenance-start'),maintenanceProgress=document.querySelector('#maintenance-progress span'),maintenanceStatus=document.querySelector('#maintenance-status'),resetMaintenance=document.querySelector('#reset-maintenance'),downloadMaintenance=document.querySelector('#download-maintenance'),maintenanceKey='dimple-maintenance-progress';function addPeriod(date,frequency){const next=new Date(date);if(frequency==='weekly')next.setDate(next.getDate()+7);if(frequency==='monthly')next.setMonth(next.getMonth()+1);if(frequency==='quarterly')next.setMonth(next.getMonth()+3);if(frequency==='annual')next.setFullYear(next.getFullYear()+1);return next;}function nextDue(start,frequency){let due=new Date(start),today=new Date();today.setHours(0,0,0,0);while(due<today)due=addPeriod(due,frequency);return due;}function formatMaintenanceDate(date){return date.toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'})}function updateMaintenance(){if(!maintenance)return;const boxes=[...maintenance.querySelectorAll('input[type="checkbox"]')],done=boxes.filter(x=>x.checked).length,percent=Math.round(done/boxes.length*100);maintenanceProgress.style.width=`${percent}%`;maintenanceStatus.textContent=`${done} of ${boxes.length} tasks marked complete (${percent}%). Completion is stored only in this browser.`;const start=maintenanceStart.value?new Date(`${maintenanceStart.value}T12:00:00`):new Date();document.querySelectorAll('[data-frequency]').forEach(group=>{const due=nextDue(start,group.dataset.frequency),time=group.querySelector('time');time.dateTime=due.toISOString().slice(0,10);time.textContent=`Next: ${formatMaintenanceDate(due)}`;});try{const state={start:maintenanceStart.value,checked:boxes.map(x=>x.checked)};localStorage.setItem(maintenanceKey,JSON.stringify(state));}catch(e){}}if(maintenance){const tomorrow=new Date();tomorrow.setDate(tomorrow.getDate()+1);maintenanceStart.value=tomorrow.toISOString().slice(0,10);try{const saved=JSON.parse(localStorage.getItem(maintenanceKey)||'null');if(saved){maintenanceStart.value=saved.start||maintenanceStart.value;[...maintenance.querySelectorAll('input[type="checkbox"]')].forEach((box,i)=>box.checked=Boolean(saved.checked?.[i]));}}catch(e){}maintenance.addEventListener('change',updateMaintenance);updateMaintenance();}if(resetMaintenance)resetMaintenance.addEventListener('click',()=>{maintenance.querySelectorAll('input[type="checkbox"]').forEach(x=>x.checked=false);try{localStorage.removeItem(maintenanceKey)}catch(e){}updateMaintenance();});if(downloadMaintenance)downloadMaintenance.addEventListener('click',()=>{const start=maintenanceStart.value.replaceAll('-',''),groups=[...document.querySelectorAll('[data-frequency]')],rules={weekly:'FREQ=WEEKLY;COUNT=52',monthly:'FREQ=MONTHLY;COUNT=12',quarterly:'FREQ=MONTHLY;INTERVAL=3;COUNT=4',annual:'FREQ=YEARLY;COUNT=1'},events=groups.flatMap((group,gi)=>[...group.querySelectorAll('.maintenance-tasks label')].map((label,ti)=>`BEGIN:VEVENT\r\nUID:dimple-maintenance-${gi}-${ti}@darshan-eeshwar.github.io\r\nDTSTART;VALUE=DATE:${start}\r\nRRULE:${rules[group.dataset.frequency]}\r\nSUMMARY:Website maintenance - ${label.textContent.trim()}\r\nDESCRIPTION:Planning reminder generated by Dimple Enterprises maintenance planner.\r\nEND:VEVENT`)).join('\r\n'),calendar=`BEGIN:VCALENDAR\r\nVERSION:2.0\r\nPRODID:-//Dimple Enterprises//Maintenance Planner//EN\r\nCALSCALE:GREGORIAN\r\n${events}\r\nEND:VCALENDAR\r\n`,blob=new Blob([calendar],{type:'text/calendar'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Website-Maintenance-Reminders.ics';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});const toolsSearch=document.querySelector('#tools-search'),toolCards=[...document.querySelectorAll('.hub-tool')],toolFilters=[...document.querySelectorAll('[data-tool-filter]')],toolsSummary=document.querySelector('#tools-summary'),recentTools=document.querySelector('#recent-tools'),toolFavKey='dimple-favourite-tools',toolRecentKey='dimple-recent-tools';let activeToolFilter='all';function savedJson(key,fallback=[]){try{return JSON.parse(localStorage.getItem(key)||JSON.stringify(fallback))}catch(e){return fallback}}function renderToolState(){const favourites=savedJson(toolFavKey),query=(toolsSearch?.value||'').trim().toLowerCase();let visible=0;toolCards.forEach(card=>{const matchFilter=activeToolFilter==='all'||activeToolFilter==='favourites'&&favourites.includes(card.dataset.tool)||card.dataset.category===activeToolFilter,matchQuery=!query||card.textContent.toLowerCase().includes(query),show=matchFilter&&matchQuery;card.hidden=!show;if(show)visible++;const button=card.querySelector('.favourite-tool'),saved=favourites.includes(card.dataset.tool);button.classList.toggle('saved',saved);button.textContent=saved?'★':'☆';button.setAttribute('aria-label',saved?'Remove from favourites':'Add to favourites');});if(toolsSummary)toolsSummary.textContent=`Showing ${visible} of ${toolCards.length} tools.`;}toolFilters.forEach(button=>button.addEventListener('click',()=>{activeToolFilter=button.dataset.toolFilter;toolFilters.forEach(x=>x.classList.toggle('active',x===button));renderToolState();}));if(toolsSearch)toolsSearch.addEventListener('input',renderToolState);document.querySelectorAll('.favourite-tool').forEach(button=>button.addEventListener('click',()=>{const id=button.closest('.hub-tool').dataset.tool;let favourites=savedJson(toolFavKey);favourites=favourites.includes(id)?favourites.filter(x=>x!==id):[...favourites,id];try{localStorage.setItem(toolFavKey,JSON.stringify(favourites))}catch(e){}renderToolState();}));document.querySelectorAll('.hub-tool a').forEach(link=>link.addEventListener('click',()=>{const card=link.closest('.hub-tool'),record={id:card.dataset.tool,title:card.querySelector('h2').textContent,url:link.getAttribute('href')};let recent=savedJson(toolRecentKey);recent=[record,...recent.filter(x=>x.id!==record.id)].slice(0,4);try{localStorage.setItem(toolRecentKey,JSON.stringify(recent))}catch(e){}}));function renderRecentTools(){if(!recentTools)return;const recent=savedJson(toolRecentKey);recentTools.replaceChildren();if(!recent.length){recentTools.hidden=true;return;}recentTools.hidden=false;const strong=document.createElement('strong');strong.textContent='Recently used: ';recentTools.append(strong);recent.forEach(item=>{const a=document.createElement('a');a.href=item.url;a.textContent=item.title;recentTools.append(a);});}if(toolCards.length){renderToolState();renderRecentTools();}const copyPlanner=document.querySelector('#homepage-copy-planner'),copyHeadline=document.querySelector('#copy-headline'),copyAudience=document.querySelector('#copy-audience'),copyPromise=document.querySelector('#copy-promise'),copyServices=document.querySelector('#copy-services'),copyProof=document.querySelector('#copy-proof'),copyCta=document.querySelector('#copy-cta'),copyFaq=document.querySelector('#copy-faq'),copyPreviewHeadline=document.querySelector('#copy-preview-headline'),copyPreviewPromise=document.querySelector('#copy-preview-promise'),copyPreviewCta=document.querySelector('#copy-preview-cta'),copyChecklist=document.querySelector('#copy-checklist'),downloadCopy=document.querySelector('#download-copy'),clearCopy=document.querySelector('#clear-copy'),copyKey='dimple-homepage-copy';function copyValues(){return{headline:copyHeadline.value.trim(),audience:copyAudience.value.trim(),promise:copyPromise.value.trim(),services:copyServices.value.trim(),proof:copyProof.value.trim(),cta:copyCta.value.trim(),faq:copyFaq.value.trim()}}function updateCopyPlanner(){if(!copyPlanner)return;const v=copyValues();copyPreviewHeadline.textContent=v.headline||'A clear outcome-focused headline';copyPreviewPromise.textContent=v.promise||'Explain what you deliver, who it helps and why it matters.';copyPreviewCta.textContent=v.cta||'Primary action';const checks=[['Headline is concise and outcome-focused',v.headline.length>=20&&v.headline.length<=70],['Target customer is identified',v.audience.length>=10],['Promise explains a useful outcome',v.promise.length>=30],['Services are specific',v.services.split(/\n|,/).filter(Boolean).length>=2],['Proof is factual and supportable',v.proof.length>=15],['Call to action uses a clear verb',v.cta.length>=5],['At least two customer questions are prepared',v.faq.split('\n').filter(Boolean).length>=2]];copyChecklist.replaceChildren(...checks.map(([text,done])=>{const li=document.createElement('li');li.className=done?'done':'todo';li.textContent=`${done?'✓':'○'} ${text}`;return li}));document.querySelectorAll('[data-copy-count]').forEach(el=>{const field=document.querySelector(el.dataset.copyCount),limit=Number(el.dataset.limit);el.textContent=`${field.value.length} / ${limit}`;});try{localStorage.setItem(copyKey,JSON.stringify(v))}catch(e){}}if(copyPlanner){try{const saved=JSON.parse(localStorage.getItem(copyKey)||'null');if(saved)Object.entries(saved).forEach(([key,value])=>{const field=document.querySelector(`#copy-${key}`);if(field)field.value=value;});}catch(e){}copyPlanner.addEventListener('input',updateCopyPlanner);updateCopyPlanner();}if(downloadCopy)downloadCopy.addEventListener('click',()=>{const v=copyValues(),markdown=`# Homepage Copy Brief\n\n## Hero headline\n${v.headline}\n\n## Target customer\n${v.audience}\n\n## Core promise\n${v.promise}\n\n## Services\n${v.services.split('\n').map(x=>x.trim()).filter(Boolean).map(x=>`- ${x}`).join('\n')}\n\n## Trust and proof\n${v.proof}\n\n## Primary call to action\n${v.cta}\n\n## FAQ ideas\n${v.faq.split('\n').map(x=>x.trim()).filter(Boolean).map(x=>`- ${x}`).join('\n')}\n\n> Review every claim for accuracy before publishing.`,blob=new Blob([markdown],{type:'text/markdown'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Homepage-Copy-Brief.md';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});if(clearCopy)clearCopy.addEventListener('click',()=>{copyPlanner.reset();try{localStorage.removeItem(copyKey)}catch(e){}updateCopyPlanner();});const imageOptimizer=document.querySelector('#image-optimizer'),imageInput=document.querySelector('#image-input'),imageWidth=document.querySelector('#image-width'),imageQuality=document.querySelector('#image-quality'),imageQualityValue=document.querySelector('#image-quality-value'),imageFormat=document.querySelector('#image-format'),optimizeImage=document.querySelector('#optimize-image'),downloadImage=document.querySelector('#download-image'),imagePreview=document.querySelector('#image-preview'),imagePlaceholder=document.querySelector('#image-placeholder'),imageOriginalStat=document.querySelector('#image-original-stat'),imageOutputStat=document.querySelector('#image-output-stat'),imageSavingStat=document.querySelector('#image-saving-stat'),imageStatus=document.querySelector('#image-status');let sourceImage=null,sourceFile=null,optimizedBlob=null,optimizedUrl=null;function formatBytes(bytes){if(!Number.isFinite(bytes))return'—';if(bytes<1024)return`${bytes} B`;if(bytes<1048576)return`${(bytes/1024).toFixed(1)} KB`;return`${(bytes/1048576).toFixed(2)} MB`}if(imageQuality)imageQuality.addEventListener('input',()=>imageQualityValue.textContent=`${imageQuality.value}%`);if(imageInput)imageInput.addEventListener('change',()=>{const file=imageInput.files?.[0];if(!file)return;if(!file.type.startsWith('image/')){imageStatus.textContent='Choose a supported image file.';return;}if(file.size>20*1024*1024){imageStatus.textContent='For reliable browser processing, choose an image under 20 MB.';return;}sourceFile=file;const url=URL.createObjectURL(file),img=new Image();img.onload=()=>{sourceImage=img;imageWidth.value=Math.min(img.naturalWidth,1920);imageWidth.max=img.naturalWidth;imageOriginalStat.textContent=`${formatBytes(file.size)} · ${img.naturalWidth}×${img.naturalHeight}`;imagePreview.src=url;imagePreview.hidden=false;imagePlaceholder.hidden=true;optimizeImage.disabled=false;downloadImage.disabled=true;imageOutputStat.textContent='Not processed';imageSavingStat.textContent='—';imageStatus.textContent='Image loaded locally. Choose settings and optimize.';};img.onerror=()=>{URL.revokeObjectURL(url);imageStatus.textContent='The browser could not read this image.';};img.src=url;});if(optimizeImage)optimizeImage.addEventListener('click',()=>{if(!sourceImage||!sourceFile)return;const maxWidth=Math.max(100,Math.min(Number(imageWidth.value)||sourceImage.naturalWidth,sourceImage.naturalWidth)),scale=maxWidth/sourceImage.naturalWidth,width=Math.round(sourceImage.naturalWidth*scale),height=Math.round(sourceImage.naturalHeight*scale),canvas=document.createElement('canvas');canvas.width=width;canvas.height=height;const ctx=canvas.getContext('2d'),type=imageFormat.value;ctx.fillStyle='#ffffff';ctx.fillRect(0,0,width,height);ctx.drawImage(sourceImage,0,0,width,height);imageStatus.textContent='Optimizing locally…';canvas.toBlob(blob=>{if(!blob){imageStatus.textContent='This browser could not create the selected format.';return;}optimizedBlob=blob;if(optimizedUrl)URL.revokeObjectURL(optimizedUrl);optimizedUrl=URL.createObjectURL(blob);imagePreview.src=optimizedUrl;const saving=Math.round((1-blob.size/sourceFile.size)*100);imageOutputStat.textContent=`${formatBytes(blob.size)} · ${width}×${height}`;imageSavingStat.textContent=saving>=0?`${saving}% smaller`:`${Math.abs(saving)}% larger`;imageStatus.textContent=saving>=0?'Optimization complete. Review and download.':'The output is larger than the original. Try a lower quality or smaller width.';downloadImage.disabled=false;},type,Number(imageQuality.value)/100);});if(downloadImage)downloadImage.addEventListener('click',()=>{if(!optimizedBlob)return;const ext=imageFormat.value==='image/webp'?'webp':'jpg',base=(sourceFile.name.replace(/\.[^.]+$/,'')||'website-image').replace(/[^a-z0-9_-]/gi,'-'),a=document.createElement('a');a.href=optimizedUrl;a.download=`${base}-optimized.${ext}`;a.click();});const socialCalendar=document.querySelector('#social-calendar'),socialCards=[...document.querySelectorAll('.social-card')],socialProgress=document.querySelector('#social-progress span'),socialStatus=document.querySelector('#social-status'),exportSocial=document.querySelector('#export-social'),resetSocial=document.querySelector('#reset-social'),socialKey='dimple-social-calendar';function socialData(){return socialCards.map(card=>({date:card.querySelector('[name="date"]').value,platform:card.querySelector('[name="platform"]').value,type:card.querySelector('[name="type"]').value,theme:card.querySelector('[name="theme"]').value.trim(),caption:card.querySelector('[name="caption"]').value.trim(),cta:card.querySelector('[name="cta"]').value.trim(),ready:card.querySelector('[name="ready"]').checked}))}function updateSocialCalendar(){if(!socialCalendar)return;const data=socialData(),ready=data.filter(x=>x.ready).length;socialProgress.style.width=`${Math.round(ready/data.length*100)}%`;socialStatus.textContent=`${ready} of ${data.length} posts marked ready. Drafts are stored only in this browser.`;socialCards.forEach(card=>{const caption=card.querySelector('[name="caption"]'),count=card.querySelector('.social-char');count.textContent=`${caption.value.length} / 500 characters`;});try{localStorage.setItem(socialKey,JSON.stringify(data))}catch(e){}}function seedSocialDates(){const today=new Date();socialCards.forEach((card,i)=>{const date=new Date(today);date.setDate(date.getDate()+i);card.querySelector('[name="date"]').value=date.toISOString().slice(0,10);});}if(socialCalendar){seedSocialDates();try{const saved=JSON.parse(localStorage.getItem(socialKey)||'null');if(saved?.length)socialCards.forEach((card,i)=>{const item=saved[i];if(!item)return;['date','platform','type','theme','caption','cta'].forEach(key=>card.querySelector(`[name="${key}"]`).value=item[key]||'');card.querySelector('[name="ready"]').checked=Boolean(item.ready);});}catch(e){}socialCalendar.addEventListener('input',updateSocialCalendar);socialCalendar.addEventListener('change',updateSocialCalendar);updateSocialCalendar();}function csvCell(value){return`"${String(value).replace(/"/g,'""')}"`}if(exportSocial)exportSocial.addEventListener('click',()=>{const header=['Date','Platform','Content type','Theme','Caption','Call to action','Ready'],rows=socialData().map(x=>[x.date,x.platform,x.type,x.theme,x.caption,x.cta,x.ready?'Yes':'No']),csv=[header,...rows].map(row=>row.map(csvCell).join(',')).join('\r\n'),blob=new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Social-Content-Calendar.csv';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});if(resetSocial)resetSocial.addEventListener('click',()=>{socialCalendar.reset();seedSocialDates();try{localStorage.removeItem(socialKey)}catch(e){}updateSocialCalendar();});const altFiles=document.querySelector('#alt-files'),altList=document.querySelector('#alt-list'),altSummary=document.querySelector('#alt-summary'),exportAlt=document.querySelector('#export-alt'),clearAlt=document.querySelector('#clear-alt');let altItems=[];function validateAlt(item){const alt=item.alt.trim(),context=item.context.trim();if(item.decorative)return{level:'good',text:'Decorative image: use an empty alt attribute (alt="").'};if(!alt)return{level:'warn',text:'Add concise alternative text or mark the image decorative.'};if(/^(image|picture|photo|graphic) of\b/i.test(alt))return{level:'warn',text:'Usually remove “image of” or “picture of” and describe the useful content directly.'};if(alt.length>125)return{level:'warn',text:'This is longer than 125 characters. Length is contextual, but consider making it more concise.'};if(!context)return{level:'warn',text:'Add the page context to confirm what information matters here.'};return{level:'good',text:'Looks like a useful starting point. Confirm it communicates the image purpose in this page context.'};}function renderAltItems(){if(!altList)return;altList.replaceChildren(...altItems.map((item,index)=>{const card=document.createElement('article');card.className='alt-card';const img=document.createElement('img');img.src=item.url;img.alt='';const fields=document.createElement('div');fields.className='alt-fields';const title=document.createElement('b');title.textContent=item.file.name;const contextLabel=document.createElement('label');contextLabel.textContent='Where and why is this image used?';const context=document.createElement('input');context.type='text';context.value=item.context;context.placeholder='Example: Service page hero showing the team at work';context.addEventListener('input',()=>{item.context=context.value;renderAltFeedback(card,item,index)});contextLabel.append(context);const altLabel=document.createElement('label');altLabel.textContent='Alternative text';const alt=document.createElement('textarea');alt.rows=3;alt.maxLength=300;alt.value=item.alt;alt.disabled=item.decorative;alt.placeholder='Describe the useful visual information in context';alt.addEventListener('input',()=>{item.alt=alt.value;renderAltFeedback(card,item,index)});altLabel.append(alt);const decorativeLabel=document.createElement('label');decorativeLabel.className='alt-decorative';const decorative=document.createElement('input');decorative.type='checkbox';decorative.checked=item.decorative;decorative.addEventListener('change',()=>{item.decorative=decorative.checked;if(item.decorative)item.alt='';renderAltItems()});decorativeLabel.append(decorative,document.createTextNode(' Decorative — intentionally use empty alt text'));const feedback=document.createElement('p');feedback.className='alt-feedback';feedback.dataset.feedback=String(index);fields.append(title,contextLabel,altLabel,decorativeLabel,feedback);card.append(img,fields);setTimeout(()=>renderAltFeedback(card,item,index),0);return card;}));const complete=altItems.filter(item=>validateAlt(item).level==='good').length;altSummary.textContent=altItems.length?`${complete} of ${altItems.length} images have a valid starting decision.`:'Choose up to 12 images to begin.';exportAlt.disabled=!altItems.length;}function renderAltFeedback(card,item,index){const feedback=card.querySelector(`[data-feedback="${index}"]`),result=validateAlt(item);feedback.className=`alt-feedback ${result.level}`;feedback.textContent=`${item.decorative?'0':item.alt.length} characters · ${result.text}`;const complete=altItems.filter(x=>validateAlt(x).level==='good').length;altSummary.textContent=`${complete} of ${altItems.length} images have a valid starting decision.`;}if(altFiles)altFiles.addEventListener('change',()=>{altItems.forEach(item=>URL.revokeObjectURL(item.url));const files=[...(altFiles.files||[])].filter(file=>file.type.startsWith('image/')).slice(0,12);altItems=files.map(file=>({file,url:URL.createObjectURL(file),context:'',alt:'',decorative:false}));renderAltItems();});function csvValue(value){return`"${String(value).replace(/"/g,'""')}"`}if(exportAlt)exportAlt.addEventListener('click',()=>{const header=['Filename','Page context','Decorative','Alt text','Validation note'],rows=altItems.map(item=>{const result=validateAlt(item);return[item.file.name,item.context,item.decorative?'Yes':'No',item.decorative?'':item.alt,result.text]}),csv=[header,...rows].map(row=>row.map(csvValue).join(',')).join('\r\n'),blob=new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Image-Alt-Text-Plan.csv';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});if(clearAlt)clearAlt.addEventListener('click',()=>{altItems.forEach(item=>URL.revokeObjectURL(item.url));altItems=[];altFiles.value='';renderAltItems();});const htmlAuditForm=document.querySelector('#html-audit-form'),htmlAuditSource=document.querySelector('#html-audit-source'),htmlAuditFile=document.querySelector('#html-audit-file'),runHtmlAudit=document.querySelector('#run-html-audit'),clearHtmlAudit=document.querySelector('#clear-html-audit'),downloadHtmlAudit=document.querySelector('#download-html-audit'),htmlAuditOverview=document.querySelector('#html-audit-overview'),htmlAuditFindings=document.querySelector('#html-audit-findings'),htmlAuditStatus=document.querySelector('#html-audit-status');let htmlAuditReport='';function finding(severity,title,detail){return{severity,title,detail}}function accessibleText(el){return(el.getAttribute('aria-label')||el.getAttribute('title')||el.textContent||[...el.querySelectorAll('img[alt]')].map(x=>x.alt).join(' ')).trim()}function auditHtml(source){const doc=new DOMParser().parseFromString(source,'text/html'),findings=[];if(!doc.documentElement.lang)findings.push(finding('error','Document language missing','Add a valid lang attribute to the <html> element.'));else findings.push(finding('pass','Document language present',`lang="${doc.documentElement.lang}"`));if(!doc.title.trim())findings.push(finding('error','Page title missing','Add a concise, unique <title>.'));else findings.push(finding('pass','Page title present',doc.title.trim()));if(!doc.querySelector('meta[name="viewport"]'))findings.push(finding('warning','Viewport metadata missing','Add responsive viewport metadata for mobile users.'));const h1=doc.querySelectorAll('h1');if(h1.length!==1)findings.push(finding(h1.length?'warning':'error','Unexpected H1 count',`Found ${h1.length}; a clear page normally has one primary heading.`));else findings.push(finding('pass','One H1 found',h1[0].textContent.trim()));let previous=0;[...doc.querySelectorAll('h1,h2,h3,h4,h5,h6')].forEach(h=>{const level=Number(h.tagName[1]);if(previous&&level>previous+1)findings.push(finding('warning','Heading level skipped',`${h.tagName} follows H${previous}: ${h.textContent.trim().slice(0,80)}`));previous=level;});const images=[...doc.querySelectorAll('img')],missingAlt=images.filter(img=>!img.hasAttribute('alt'));if(missingAlt.length)findings.push(finding('error','Images missing alt attributes',`${missingAlt.length} of ${images.length} images do not have an alt attribute.`));else findings.push(finding('pass','Image alt attributes present',`${images.length} images checked.`));const controls=[...doc.querySelectorAll('input:not([type="hidden"]),select,textarea')],unlabelled=controls.filter(el=>!(el.labels?.length||el.getAttribute('aria-label')||el.getAttribute('aria-labelledby')));if(unlabelled.length)findings.push(finding('error','Form controls without labels',`${unlabelled.length} of ${controls.length} controls lack a label or accessible-name reference.`));else if(controls.length)findings.push(finding('pass','Form controls have label mechanisms',`${controls.length} controls checked.`));const unnamedButtons=[...doc.querySelectorAll('button,input[type="button"],input[type="submit"]')].filter(el=>el.tagName==='INPUT'?!el.value&&!el.getAttribute('aria-label'):!accessibleText(el));if(unnamedButtons.length)findings.push(finding('error','Buttons without accessible names',`${unnamedButtons.length} buttons need visible text or an accessible name.`));const links=[...doc.querySelectorAll('a[href]')],unnamedLinks=links.filter(link=>!accessibleText(link));if(unnamedLinks.length)findings.push(finding('error','Links without accessible names',`${unnamedLinks.length} links have no useful name.`));const ids=[...doc.querySelectorAll('[id]')].map(el=>el.id),duplicates=[...new Set(ids.filter((id,i)=>ids.indexOf(id)!==i))];if(duplicates.length)findings.push(finding('error','Duplicate IDs found',duplicates.slice(0,10).join(', ')));else findings.push(finding('pass','No duplicate IDs detected',`${ids.length} IDs checked.`));if(!doc.querySelector('main,[role="main"]'))findings.push(finding('warning','Main landmark missing','Add a <main> element or role="main".'));else findings.push(finding('pass','Main landmark present','A primary content landmark was found.'));if(!doc.querySelector('html'))findings.push(finding('error','HTML document not detected','Paste a complete HTML document for the most useful review.'));return findings;}function renderHtmlAudit(){const source=htmlAuditSource.value.trim();if(!source){htmlAuditStatus.textContent='Paste HTML or choose a local file first.';return;}const findings=auditHtml(source),counts={error:0,warning:0,pass:0};findings.forEach(x=>counts[x.severity]++);htmlAuditOverview.innerHTML=`<div class="audit-metric"><span>Checks</span><b>${findings.length}</b></div><div class="audit-metric"><span>Errors</span><b>${counts.error}</b></div><div class="audit-metric"><span>Warnings</span><b>${counts.warning}</b></div><div class="audit-metric"><span>Passes</span><b>${counts.pass}</b></div>`;htmlAuditFindings.replaceChildren(...findings.map(item=>{const article=document.createElement('article');article.className=`audit-finding ${item.severity}`;const title=document.createElement('b'),detail=document.createElement('p');title.textContent=`${item.severity.toUpperCase()}: ${item.title}`;detail.textContent=item.detail;article.append(title,detail);return article;}));htmlAuditStatus.textContent=`Review complete: ${counts.error} errors, ${counts.warning} warnings and ${counts.pass} passes.`;htmlAuditReport=`DIMPLE ENTERPRISES - BASIC HTML ACCESSIBILITY REVIEW\n\n${htmlAuditStatus.textContent}\n\n${findings.map((x,i)=>`${i+1}. [${x.severity.toUpperCase()}] ${x.title}\n   ${x.detail}`).join('\n\n')}\n\nThis automated structural check is not a WCAG audit or certification. Manual keyboard, screen-reader, zoom, colour, motion and usability testing are still required.`;downloadHtmlAudit.disabled=false;}if(runHtmlAudit)runHtmlAudit.addEventListener('click',renderHtmlAudit);if(htmlAuditFile)htmlAuditFile.addEventListener('change',()=>{const file=htmlAuditFile.files?.[0];if(!file)return;if(file.size>2*1024*1024){htmlAuditStatus.textContent='Choose an HTML file under 2 MB.';return;}file.text().then(text=>{htmlAuditSource.value=text;htmlAuditStatus.textContent='Local file loaded. Run the check when ready.';});});if(clearHtmlAudit)clearHtmlAudit.addEventListener('click',()=>{htmlAuditSource.value='';htmlAuditFile.value='';htmlAuditOverview.replaceChildren();htmlAuditFindings.replaceChildren();htmlAuditStatus.textContent='Paste HTML or choose a local file.';downloadHtmlAudit.disabled=true;htmlAuditReport='';});if(downloadHtmlAudit)downloadHtmlAudit.addEventListener('click',()=>{if(htmlAuditReport)downloadText('HTML-Accessibility-Review.txt',htmlAuditReport)});;const seoMetaSource=document.querySelector('#seoMetaSource'),runSeoMeta=document.querySelector('#runSeoMeta'),clearSeoMeta=document.querySelector('#clearSeoMeta'),seoMetaScore=document.querySelector('#seoMetaScore'),seoMetaChecks=document.querySelector('#seoMetaChecks'),seoMetaCode=document.querySelector('#seoMetaCode'),copySeoMeta=document.querySelector('#copySeoMeta'),seoMetaStatus=document.querySelector('#seoMetaStatus');if(runSeoMeta&&seoMetaSource){const esc=s=>String(s||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');const q=(doc,sel,attr)=>{const el=doc.querySelector(sel);return el?(attr?el.getAttribute(attr)||'':el.textContent.trim()):''};const tag=(name,content,property=false)=>content?'<meta '+(property?'property':'name')+'="'+name+'" content="'+String(content).replace(/"/g,'&quot;')+'">':'';runSeoMeta.addEventListener('click',()=>{const source=seoMetaSource.value.trim();if(!source){seoMetaStatus.textContent='Paste a full HTML document or its head section first.';return;}const doc=new DOMParser().parseFromString(source,'text/html');const data={title:q(doc,'title'),description:q(doc,'meta[name="description"]','content'),canonical:q(doc,'link[rel="canonical"]','href'),robots:q(doc,'meta[name="robots"]','content'),ogTitle:q(doc,'meta[property="og:title"]','content'),ogDescription:q(doc,'meta[property="og:description"]','content'),ogImage:q(doc,'meta[property="og:image"]','content'),ogUrl:q(doc,'meta[property="og:url"]','content'),twitterCard:q(doc,'meta[name="twitter:card"]','content'),twitterTitle:q(doc,'meta[name="twitter:title"]','content'),twitterDescription:q(doc,'meta[name="twitter:description"]','content'),twitterImage:q(doc,'meta[name="twitter:image"]','content')};const checks=[];const add=(ok,level,title,detail)=>checks.push({state:ok?'pass':level,title,detail});add(data.title.length>=30&&data.title.length<=60,'warning','Page title',data.title?data.title.length+' characters; aim for 30–60.':'Missing title tag.');add(data.description.length>=120&&data.description.length<=160,'warning','Meta description',data.description?data.description.length+' characters; aim for 120–160.':'Missing meta description.');add(/^https:\/\//i.test(data.canonical),'error','Canonical URL',data.canonical||'Missing canonical link.');add(!!data.robots,'warning','Robots directive',data.robots||'No robots meta tag found; search-engine defaults will apply.');add(!!(data.ogTitle&&data.ogDescription&&data.ogImage),'warning','Open Graph',data.ogTitle&&data.ogDescription&&data.ogImage?'Core Open Graph tags found.':'Add og:title, og:description and og:image.');add(!!data.twitterCard,'warning','Twitter/X card',data.twitterCard||'Add twitter:card for richer shares.');if(data.ogImage)add(/^https:\/\//i.test(data.ogImage),'warning','Social image URL','Use an absolute HTTPS image URL.');const passed=checks.filter(x=>x.state==='pass').length,score=Math.round(passed/checks.length*100);seoMetaScore.innerHTML='<b>'+score+'/100</b><span>'+passed+' of '+checks.length+' checks passed</span>';seoMetaChecks.innerHTML=checks.map(x=>'<div class="meta-check '+x.state+'"><b>'+esc(x.title)+'</b><span>'+esc(x.detail)+'</span></div>').join('');const title=data.title||'Your page title | Brand name',description=data.description||'Write a clear 120–160 character summary explaining the page value and the action a visitor should take.',canonical=data.canonical||'https://example.com/page',image=data.ogImage||'https://example.com/social-image.jpg';const code=['<title>'+title.replace(/</g,'&lt;')+'</title>','<meta name="description" content="'+description.replace(/"/g,'&quot;')+'">','<link rel="canonical" href="'+canonical.replace(/"/g,'&quot;')+'">',tag('robots',data.robots||'index,follow'),tag('og:type','website',true),tag('og:title',data.ogTitle||title,true),tag('og:description',data.ogDescription||description,true),tag('og:url',data.ogUrl||canonical,true),tag('og:image',image,true),tag('twitter:card',data.twitterCard||'summary_large_image'),tag('twitter:title',data.twitterTitle||title),tag('twitter:description',data.twitterDescription||description),tag('twitter:image',data.twitterImage||image)].filter(Boolean).join('\n');seoMetaCode.textContent=code;copySeoMeta.disabled=false;copySeoMeta.dataset.code=code;seoMetaStatus.textContent='Analysis complete. Review recommendations before publishing.';});clearSeoMeta.addEventListener('click',()=>{seoMetaSource.value='';seoMetaScore.innerHTML='';seoMetaChecks.innerHTML='';seoMetaCode.textContent='Recommended tags will appear here.';copySeoMeta.disabled=true;copySeoMeta.dataset.code='';seoMetaStatus.textContent='Your HTML stays in this browser.';});copySeoMeta.addEventListener('click',async()=>{const code=copySeoMeta.dataset.code||'';if(!code)return;try{await navigator.clipboard.writeText(code);seoMetaStatus.textContent='Recommended meta tags copied.';}catch(e){seoMetaStatus.textContent='Copy was blocked. Select the code and copy it manually.';}});};const schemaForm=document.querySelector('#schemaForm'),schemaOutput=document.querySelector('#schemaOutput'),schemaStatus=document.querySelector('#schemaStatus'),copySchema=document.querySelector('#copySchema'),downloadSchema=document.querySelector('#downloadSchema'),resetSchema=document.querySelector('#resetSchema');if(schemaForm&&schemaOutput){let currentSchema='';const val=id=>(document.querySelector('#'+id)?.value||'').trim();const buildSchema=()=>{const errors=[],name=val('schemaName'),url=val('schemaUrl'),phone=val('schemaPhone'),email=val('schemaEmail'),street=val('schemaStreet'),city=val('schemaCity'),region=val('schemaRegion'),postal=val('schemaPostal'),country=val('schemaCountry'),area=val('schemaArea'),price=val('schemaPrice'),opens=val('schemaOpens'),closes=val('schemaCloses'),days=[...document.querySelectorAll('input[name="schemaDays"]:checked')].map(x=>x.value);if(!name)errors.push('Business name is required.');if(!/^https:\/\//i.test(url))errors.push('Website URL must be an absolute HTTPS URL.');if(email&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))errors.push('Enter a valid email address.');if(!street||!city||!region||!postal||!country)errors.push('Complete the full postal address.');const data={'@context':'https://schema.org','@type':val('schemaType')||'LocalBusiness',name,url};if(phone)data.telephone=phone;if(email)data.email=email;if(price)data.priceRange=price;if(area)data.areaServed=area.split(',').map(x=>x.trim()).filter(Boolean);data.address={'@type':'PostalAddress',streetAddress:street,addressLocality:city,addressRegion:region,postalCode:postal,addressCountry:country};if(opens&&closes&&days.length)data.openingHoursSpecification=[{'@type':'OpeningHoursSpecification',dayOfWeek:days,opens,closes}];const sameAs=val('schemaSocial').split('\n').map(x=>x.trim()).filter(Boolean);if(sameAs.length)data.sameAs=sameAs;currentSchema='<script type="application/ld+json">\n'+JSON.stringify(data,null,2)+'\n<\/script>';schemaOutput.textContent=currentSchema;copySchema.disabled=downloadSchema.disabled=errors.length>0;schemaStatus.className=errors.length?'schema-errors':'schema-valid';schemaStatus.textContent=errors.length?errors.join(' '):'Schema is ready. Test it with Google Rich Results Test before publishing.';};schemaForm.addEventListener('submit',e=>{e.preventDefault();buildSchema();});copySchema.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(currentSchema);schemaStatus.textContent='JSON-LD copied.';}catch(e){schemaStatus.textContent='Copy was blocked. Select the code and copy it manually.';}});downloadSchema.addEventListener('click',()=>{if(currentSchema)downloadText('local-business-schema.html',currentSchema);});resetSchema.addEventListener('click',()=>{schemaForm.reset();schemaOutput.textContent='Generated JSON-LD will appear here.';schemaStatus.className='helper-note';schemaStatus.textContent='Your details remain in this browser.';copySchema.disabled=downloadSchema.disabled=true;currentSchema='';});};const robotsForm=document.querySelector('#robotsForm'),robotsOutput=document.querySelector('#robotsOutput'),robotsStatus=document.querySelector('#robotsStatus'),robotsPreset=document.querySelectorAll('.robots-preset'),copyRobots=document.querySelector('#copyRobots'),downloadRobots=document.querySelector('#downloadRobots');if(robotsForm&&robotsOutput){let robotsText='';const applyPreset=mode=>{const rules=document.querySelector('#robotsRules');if(mode==='public')rules.value='Allow: /';if(mode==='protect')rules.value='Allow: /\nDisallow: /admin/\nDisallow: /private/\nDisallow: /checkout/\nDisallow: /*?*';if(mode==='staging')rules.value='Disallow: /';robotsPreset.forEach(x=>x.setAttribute('aria-pressed',String(x.dataset.preset===mode)));};robotsPreset.forEach(x=>x.addEventListener('click',()=>applyPreset(x.dataset.preset)));robotsForm.addEventListener('submit',e=>{e.preventDefault();const agent=(document.querySelector('#robotsAgent').value||'*').trim(),rules=document.querySelector('#robotsRules').value.split('\n').map(x=>x.trim()).filter(Boolean),sitemap=document.querySelector('#robotsSitemap').value.trim(),host=document.querySelector('#robotsHost').value.trim(),warnings=[];if(!rules.length)warnings.push('Add at least one Allow or Disallow rule.');rules.forEach((line,i)=>{if(!/^(Allow|Disallow|Crawl-delay):\s*/i.test(line))warnings.push('Line '+(i+1)+' is not a recognised directive.');});if(rules.some(x=>/^Disallow:\s*\/$/i.test(x)))warnings.push('This blocks the entire site from crawling. Use only for a non-public environment.');if(rules.some(x=>/^Disallow:\s*\/\*?$/i.test(x)))warnings.push('A broad Disallow rule may hide all pages from search engines.');if(sitemap&&!/^https:\/\//i.test(sitemap))warnings.push('Sitemap must be an absolute HTTPS URL.');if(host&&!/^https?:\/\//i.test(host))warnings.push('Host should be an absolute URL or left empty.');robotsText=['User-agent: '+agent,...rules,sitemap?'':''].filter((x,i,a)=>x!==''||i<a.length-1).join('\n');if(sitemap)robotsText+='\n\nSitemap: '+sitemap;if(host)robotsText+='\nHost: '+host;robotsOutput.textContent=robotsText;copyRobots.disabled=downloadRobots.disabled=warnings.length>0;robotsStatus.className=warnings.length?'robots-warning':'robots-safe';robotsStatus.textContent=warnings.length?warnings.join(' '):'No obvious conflicts found. Review the rules before publishing at /robots.txt.';});copyRobots.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(robotsText);robotsStatus.textContent='robots.txt copied.';}catch(e){robotsStatus.textContent='Copy was blocked. Select the text and copy it manually.';}});downloadRobots.addEventListener('click',()=>{if(robotsText)downloadText('robots.txt',robotsText);});applyPreset('protect');};const sitemapBuilder=document.querySelector('#sitemapBuilder'),sitemapEntries=document.querySelector('#sitemapEntries'),sitemapOutput=document.querySelector('#sitemapOutput'),sitemapStatus=document.querySelector('#sitemapStatus'),sitemapSummary=document.querySelector('#sitemapSummary'),sitemapIssues=document.querySelector('#sitemapIssues'),copySitemap=document.querySelector('#copySitemap'),downloadSitemap=document.querySelector('#downloadSitemap'),sampleSitemap=document.querySelector('#sampleSitemap');if(sitemapBuilder&&sitemapEntries){let xml='';const x=s=>String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&apos;');sampleSitemap.addEventListener('click',()=>{const today=new Date().toISOString().slice(0,10);sitemapEntries.value='https://example.com/ | weekly | 1.0 | '+today+'\nhttps://example.com/services | monthly | 0.8 | '+today+'\nhttps://example.com/contact | yearly | 0.6 | '+today;});sitemapBuilder.addEventListener('submit',e=>{e.preventDefault();const lines=sitemapEntries.value.split('\n').map(x=>x.trim()).filter(Boolean),items=[],issues=[],seen=new Set(),freqs=new Set(['always','hourly','daily','weekly','monthly','yearly','never']);let origin='';lines.forEach((line,i)=>{const [loc='',freq='monthly',priority='0.5',lastmod='']=line.split('|').map(x=>x.trim());let url;try{url=new URL(loc);if(url.protocol!=='https:')issues.push('Line '+(i+1)+': use an HTTPS URL.');if(!origin)origin=url.origin;else if(url.origin!==origin)issues.push('Line '+(i+1)+': URL uses a different domain.');}catch(err){issues.push('Line '+(i+1)+': invalid absolute URL.');return;}const clean=url.href;if(seen.has(clean)){issues.push('Line '+(i+1)+': duplicate URL.');return;}seen.add(clean);if(!freqs.has(freq.toLowerCase()))issues.push('Line '+(i+1)+': invalid change frequency.');const p=Number(priority);if(!Number.isFinite(p)||p<0||p>1)issues.push('Line '+(i+1)+': priority must be between 0.0 and 1.0.');if(lastmod&&!/^\d{4}-\d{2}-\d{2}$/.test(lastmod))issues.push('Line '+(i+1)+': last modified date must be YYYY-MM-DD.');items.push({loc:clean,freq:freq.toLowerCase(),priority,lastmod});});xml='<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n'+items.map(o=>'  <url>\n    <loc>'+x(o.loc)+'</loc>\n    <changefreq>'+x(o.freq)+'</changefreq>\n    <priority>'+x(o.priority)+'</priority>'+(o.lastmod?'\n    <lastmod>'+x(o.lastmod)+'</lastmod>':'')+'\n  </url>').join('\n')+'\n</urlset>';sitemapOutput.textContent=xml;sitemapSummary.innerHTML='<div class="sitemap-stat"><b>'+items.length+'</b><span>valid URLs</span></div><div class="sitemap-stat"><b>'+issues.length+'</b><span>warnings</span></div><div class="sitemap-stat"><b>'+new Blob([xml]).size+'</b><span>bytes</span></div>';sitemapIssues.innerHTML=issues.map(i=>'<div class="sitemap-issue">'+x(i)+'</div>').join('');copySitemap.disabled=downloadSitemap.disabled=!items.length||issues.length>0;sitemapStatus.className=issues.length?'robots-warning':'robots-safe';sitemapStatus.textContent=issues.length?'Fix all warnings before downloading.':'Sitemap is ready. Upload it at your site root and submit its URL to search engines.';});copySitemap.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(xml);sitemapStatus.textContent='XML sitemap copied.';}catch(e){sitemapStatus.textContent='Copy was blocked. Select the XML and copy it manually.';}});downloadSitemap.addEventListener('click',()=>{if(xml)downloadText('sitemap.xml',xml);});};const faqSchemaForm=document.querySelector('#faqSchemaForm'),faqSchemaList=document.querySelector('#faqSchemaList'),addFaqSchema=document.querySelector('#addFaqSchema'),faqSchemaOutput=document.querySelector('#faqSchemaOutput'),faqSchemaStatus=document.querySelector('#faqSchemaStatus'),faqSchemaCount=document.querySelector('#faqSchemaCount'),copyFaqSchema=document.querySelector('#copyFaqSchema'),downloadFaqSchema=document.querySelector('#downloadFaqSchema');if(faqSchemaForm&&faqSchemaList){let faqJson='';const makeRow=(q='',a='')=>{const row=document.createElement('div');row.className='faq-schema-row';row.innerHTML='<div class="faq-row-head"><b>FAQ item</b><button class="faq-remove" type="button">Remove</button></div><label>Question<input class="faq-q" maxlength="250"></label><label>Answer<textarea class="faq-a" maxlength="2000"></textarea></label>';row.querySelector('.faq-q').value=q;row.querySelector('.faq-a').value=a;row.querySelector('.faq-remove').addEventListener('click',()=>{if(faqSchemaList.children.length>1)row.remove();faqSchemaCount.textContent=faqSchemaList.children.length+' items';});faqSchemaList.appendChild(row);faqSchemaCount.textContent=faqSchemaList.children.length+' items';};addFaqSchema.addEventListener('click',()=>makeRow());faqSchemaForm.addEventListener('submit',e=>{e.preventDefault();const rows=[...faqSchemaList.querySelectorAll('.faq-schema-row')],entries=[],issues=[],seen=new Set();rows.forEach((row,i)=>{const q=row.querySelector('.faq-q').value.trim(),a=row.querySelector('.faq-a').value.trim();if(!q||!a){issues.push('Item '+(i+1)+': both question and answer are required.');return;}const key=q.toLowerCase().replace(/\s+/g,' ');if(seen.has(key)){issues.push('Item '+(i+1)+': duplicate question.');return;}seen.add(key);if(q.length<10)issues.push('Item '+(i+1)+': question is unusually short.');if(a.length<20)issues.push('Item '+(i+1)+': answer should be more complete.');entries.push({'@type':'Question',name:q,acceptedAnswer:{'@type':'Answer',text:a}});});const data={'@context':'https://schema.org','@type':'FAQPage',mainEntity:entries};faqJson='<script type="application/ld+json">\n'+JSON.stringify(data,null,2)+'\n<\/script>';faqSchemaOutput.textContent=faqJson;copyFaqSchema.disabled=downloadFaqSchema.disabled=!entries.length||issues.length>0;faqSchemaStatus.className=issues.length?'robots-warning':'robots-safe';faqSchemaStatus.textContent=issues.length?issues.join(' '):entries.length+' FAQ items validated. Ensure every answer is visible on the page before publishing.';});copyFaqSchema.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(faqJson);faqSchemaStatus.textContent='FAQ JSON-LD copied.';}catch(e){faqSchemaStatus.textContent='Copy was blocked. Select the code and copy it manually.';}});downloadFaqSchema.addEventListener('click',()=>{if(faqJson)downloadText('faq-schema.html',faqJson);});if(!faqSchemaList.children.length){makeRow('How long does a business website take?','A typical small-business website takes about 2–4 weeks after content and approvals are available.');makeRow('Will the website work well on mobile?','Yes. Every new website is designed and tested for mobile, tablet and desktop use.');}}